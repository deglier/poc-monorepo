import { css } from '@ui/web'
import { colors } from '@ui/colors'

export const Input = css({
  all: 'unset',
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  borderRadius: '$sm',
  boxShadow: `0 0 0 1px ${colors['neutral-light-deep']}}`,
  py: '$4',
  px: '$4',
})
